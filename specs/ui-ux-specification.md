# UI/UX Specification: Hexagon Strategy Game (v1.2)

## 1. Концепція

- **Тема:** Dark glassmorphism — темний фон, напівпрозорий сайдбар, яскраві неонові акценти.
- **Шрифти:** Outfit (заголовки, HUD), Inter (текст, журнал) — Google Fonts.
- **Фон:** `#1a1c2c` (dark navy).

---

## 2. Кольорова палітра

| Елемент | HEX |
|---------|-----|
| Гравець | `#00d2ff` |
| ШІ | `#ff416c` |
| Нейтральний гекс | `#95a5a6` |
| Вибраний гекс (обводка) | `#f1c40f` |
| Темний вибраний | 30% opacity |
| Атака (гравець) | `rgba(0, 210, 255, α)` |
| Атака (ШІ) | `rgba(255, 65, 108, α)` |

---

## 3. Компоненти UI

### 3.1 Головне меню (`#main-menu`)
- Overlay поверх гри, прихований після старту.
- Заголовок + підзаголовок.
- **Вибір складності:** 4 радіо-кнопки з бейджами кольорів.
- Кнопка «Почати гру».
- Версія внизу.

### 3.2 Шапка (`header.app-header`)
- Ліворуч: Хід N, кількість територій гравця | ШІ.
- Праворуч: Панель підкріплень (прозірна 30% у фазі атаки).

### 3.3 Сайдбар (`aside.sidebar`) — повна висота, ліворуч
- **Заголовок** з назвою «Історія битв» + індикатор «ШІ думає…» (анімована точка, прихована при ході гравця).
- **Журнал подій** (`#game-log`): scrollable, авто-прокрутка донизу, max 50 записів.
- **Панель ходів** (`#move-history`): collapsible (кнопка ▼/▲), відображає до 50 останніх ходів у зворотному порядку з іконками:
  - ⚔️ — захоплення
  - 💥 — пошкодження
  - 🛡️ — відбита атака
  - ➡️ — переміщення
- **Підвал:** кнопки «Завершити хід» та «Нова гра».

### 3.4 Ігровий Canvas (`canvas#game-canvas`)
- Займає весь `main.game-container`.
- Центрована камера (`camera.x = width/2`, `camera.y = height/2`).
- Responsive: ресайзиться при `window.resize`.

### 3.5 Діалог переміщення (`#troop-dialog`)
- Плаваючий контейнер, позиціонується відносно цільового гексу.
- Показує: джерело → ціль, доступно N військ.
- Слайдер `range` від 1 до `units-1`.
- Кнопки «Перемістити» / «Скасувати».
- Автоматично зсувається якщо виходить за межі екрана.

### 3.6 Game Over Overlay (`#game-over`)
- Прихований overlay, з'являється при `status !== 'ongoing'`.
- **Перемога:** заголовок «ПЕРЕМОГА!» з blue-gradient.
- **Поразка:** заголовок «ПОРАЗКА» з red-gradient.
- 3 секції статистики (гравець / ШІ / загальна) в кольорових картках.
- Кнопка «Грати знову».

---

## 4. Гекс canvas-рендеринг

### На кожному гексі відображається:
| Позиція | Вміст |
|---------|-------|
| Верхня частина | Координата `q` |
| Центр | Координата `r` |
| Нижня частина | Кількість одиниць (жирний білий, 16px) |

### Стан гексів:
- **Активний** (власний або нейтральний): `opacity = 0.8 + flash*0.2`
- **Неактивний** (ворожий): `opacity = 0.3`
- **Вибраний:** жовта пульсуюча обводка, `lineWidth = 3`
- **Flash:** білий шар `opacity = flash*0.5`, затухає `−0.05` за кадр

---

## 5. Анімації

| Тип | Ефект |
|-----|-------|
| Атака | Штрихована лінія + expanding ring у кольорі атакуючого |
| Переміщення | Куля переміщується по кривій + іскри (30% chance/frame) |
| Вибух | 30 частинок від точки попадання |
| Конфетті | 150 mix-type частинок при перемозі |
| ШІ-індикатор | CSS pulse-анімація крапки |

---

## 6. User Flows

### 6.1 Старт гри
1. Гравець бачить головне меню.
2. Обирає складність (default: Normal).
3. Клікає «Почати гру» → меню зникає, карта активна.

### 6.2 Хід гравця — підкріплення
1. Шапка показує кількість доступних військ.
2. Гравець кліка на свій гекс → +1 військо, -1 доступне.
3. Коли 0 залишилось — автоматично перехід до атаки.

### 6.3 Хід гравця — атака
1. Клік на свій гекс (>1 od) → пульсуюча обводка.
2. Клік на сусідній ворожий → анімація атаки → результат у журналі.
3. Клік на сусідній свій → діалог вибору кількості → «Перемістити».

### 6.4 Хід ШІ
1. «Завершити хід» → з'являється «ШІ думає…».
2. ШІ виконує дії (з відчутними паузами).
3. Після завершення — знову хід гравця.

### 6.5 Кінець гри
1. При захопленні всіх / втраті всіх → overlay.
2. Конфетті (при перемозі).
3. Статистика → «Грати знову».
